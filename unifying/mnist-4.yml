method: grid
name: resnet-init-scale-sweep
program: mnist_resnet.py
project: mnist-grokking
parameters:
    num_training_steps:
        value: 25000
    init_scale: 
        values: [1.0, 6.]
    frac_train:
        values: [0.02, 0.04]
    frac_label_noise:
        values: [0.2, 0.4]
    channel_growth:
        value: 8    
